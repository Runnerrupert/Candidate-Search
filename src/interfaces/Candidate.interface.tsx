// Candidate interface with all required data
interface Candidate {
    avatar_url: string;
    name: string;
    login: string;
    company: string;
    location: string;
    email: string;
    bio: string;
}

export default Candidate;